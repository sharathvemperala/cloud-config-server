eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost
    
 payment-service-url: http://PAYMENT-SERVICE/payment/payments
 order-service-url: http://ORDER-SERVICE/order/orders

